<!DOCTYPE html>
<html>
  <head>
    <title>Petsy</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="<%= root_path %>">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">Users</a>
            </li>
            <!--li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Dropdown
              </a>
              <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="#">Action</a></li>
                <li><a class="dropdown-item" href="#">Another action</a></li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item" href="#">Something else here</a></li>
              </ul>
            </li-->
          </ul>
          <% if user_signed_in? %>
            <a href="#" class="btn btn-primary">
              <% if @user.avatar %>
                <%= image_tag @user.avatar_url, width:"50" %>
              <% end %>
              <%= current_user.username %>
            </a>
            <ul>
              <li>
                <%= link_to "Mon profil", profil_path %>
              </li>
              <li>
                <%= link_to "Se déco", destroy_session_path, data: {turbo_method: "delete", turbo_confirm: "Are you sure ?"} %>
              </li>
              <% if isAdmin? %>
                <li>
                  <%= link_to "Gestion des espèces", new_admin_species_path %>
                </li>
              <% end %>
            </ul>
          <% else %>
            <form class="d-flex" role="search">
              <a class="btn btn-primary" href="<%= new_user_path %>">S'inscrire</a>
              <a class="btn btn-secondary" href="<%= new_session_path %>">Se connecter</a>
            </form>
            <% end %>
        </div>
      </div>
    </nav>


  
    <div class="container">
      <% flash.each do |key,value| %>
        <div class="alert alert-<%= key %>">
          <%= value %>
        </div>
      <% end %>


      
      <%= yield %>



      <%= debug session[:auth] %>
    </div>



    
  </body>
</html>
